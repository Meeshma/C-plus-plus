#include <iostream>

int main() {
    // Loop to print numbers from 1 to 10
    for (int i = 1; i <= 10; ++i) {
        std::cout << i << " ";
    }
    std::cout << std::endl; // Print a newline at the end

    return 0;
}
